/// <reference types="bun-types" />

import { describe, test } from "bun:test";

describe("bin wrapper", () => {
  test("platform resolution is tested in platform.test.ts", () => {
    // Lazycodex bin wrapper tests were removed with omo-codex.
    // Platform binary resolution is covered by platform.test.ts.
  });
});
